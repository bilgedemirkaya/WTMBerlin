{
  "name": "week3",
  "version": "1.0.0",
  "description": "Static functions are functions on the classes that you can call.Just regular functions attached to the class but **instances you create cannot acces them**. They are at the higher level.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "chalk": "^4.1.0",
    "circular-json": "^0.5.9",
    "flatted": "^3.1.0"
  },
  "devDependencies": {
    "@babel/helper-compilation-targets": "^7.12.5"
  }
}
